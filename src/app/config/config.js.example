var config = {};

// @Change add configurations
// config.foo = 'bar';
config.relayPath = 'http://localhost:3000/api/graphql';
config.relayHeaders = {
  'X-Lapis-Token': 'dev'
}

module.exports = config;
