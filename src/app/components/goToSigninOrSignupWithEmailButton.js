var React = require('react')

var GoToSigninOrSignupWithEmailButton = React.createClass({
  render: function () {
    return(
      <a href="" >Sign in or sign up with email</a>
    )
  }
})

module.exports = GoToSigninOrSignupWithEmailButton;
