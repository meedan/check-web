import React from 'react';

const Tab = (
  active,
  disabled,
  iconCenter,
  iconLeft,
  iconRight,
  label,
  size,
) => {
  return (
    <div>
      {label}
    </div>
  );
};

export default Tab;