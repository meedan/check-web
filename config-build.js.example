const config = {
  "transifex": {
    "user": "",
    "password": "",
    "project": "",
    "languages": ["ar", "es", "fr", "pt"]
  },

  // Path or URL to the relay.json file that is generated by the backend.
  // For local development, use something like "/api/public/relay.json".
  "relay": "https://raw.githubusercontent.com/meedan/check-api/develop/public/relay.json"
};
module.exports = config;
