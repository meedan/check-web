const config = {

  // Transifex is used to localize strings https://transifex.com
  //
  // OPTIONAL
  // Only needed for tasks `npm run transifex:XXX`
  // By default, latest localizations are included in the codebase
  //
  // Offical Check localization at https://transifex.com/meedan/check-2
  // The user should have a Transifex role of Project Maintainer
  //
  "transifex": {
    "user": "<TRANSIFEX USERNAME>",
    "password": "<TRANSIFEX PASSWORD>",
    "project": "check-2",
    "languages": ["ar", "es", "fr", "id", "mk", "pt", "ru", "ro", "sw", "fil"]
  },

  // Path or URL to the GraphQL relay.json file that is generated by the backend
  //
  // REQUIRED
  // The value below is sufficient for local development
  //
  "relay": "/api/public/relay.json",
};
module.exports = config;
